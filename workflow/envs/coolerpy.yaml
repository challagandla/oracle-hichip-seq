# cooler-backed Python analysis: APA, per-loop counts, virtual 4C,
# compartment bigWig export (stages 06, 07, 08)
name: oracle-hichip-coolerpy
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - cooler=0.10.*
  - pybigwig=0.3.*
  - matplotlib=3.9.*
  - numpy=1.26.*
  - pandas=2.2.*
